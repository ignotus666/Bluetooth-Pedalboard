//BANK 7:

void bank7()
{
  if (key1Pressed)
  {
    MIDI.sendProgramChange(36, 1);
    midiLed();

    foot1();
    bank7Presets();

    activePreset = 37;
    return2ActivePreset();
    key1Pressed = false;
  }

  if (key2Pressed)
  {
    MIDI.sendProgramChange(37, 1);
    midiLed();

    foot2();
    bank7Presets();

    activePreset = 38;
    return2ActivePreset();
    key2Pressed = false;
  }

  if (key3Pressed)
  {
    MIDI.sendProgramChange(38, 1);
    midiLed();

    foot3();
    bank7Presets();

    activePreset = 39;
    return2ActivePreset();
    key3Pressed = false;
  }

  if (key4Pressed)
  {
    MIDI.sendProgramChange(39, 1);
    midiLed();

    foot4();
    bank7Presets();

    activePreset = 40;
    return2ActivePreset();
    key4Pressed = false;
  }

  if (key5Pressed)
  {
    MIDI.sendProgramChange(40, 1);
    midiLed();

    foot5();
    bank7Presets();

    activePreset = 41;
    return2ActivePreset();
    key5Pressed = false;
  }

  if (key6Pressed)
  {
    MIDI.sendProgramChange(41, 1);
    midiLed();

    foot6();
    bank7Presets();

    activePreset = 42;
    return2ActivePreset();
    key6Pressed = false;
  }
}

//BANK 8:

void bank8()
{
  if (key1Pressed)
  {
    MIDI.sendProgramChange(42, 1);
    midiLed();

    foot1();
    bank8Presets();

    activePreset = 43;
    return2ActivePreset();
    key1Pressed = false;
  }

  if (key2Pressed)
  {
    MIDI.sendProgramChange(43, 1);
    midiLed();

    foot2();
    bank8Presets();

    activePreset = 44;
    return2ActivePreset();
    key2Pressed = false;
  }

  if (key3Pressed)
  {
    MIDI.sendProgramChange(44, 1);
    midiLed();

    foot3();
    bank8Presets();

    activePreset = 45;
    return2ActivePreset();
    key3Pressed = false;
  }

  if (key4Pressed)
  {
    MIDI.sendProgramChange(45, 1);
    midiLed();

    foot4();
    bank8Presets();

    activePreset = 46;
    return2ActivePreset();
    key4Pressed = false;
  }

  if (key5Pressed)
  {
    MIDI.sendProgramChange(46, 1);
    midiLed();

    foot5();
    bank8Presets();

    activePreset = 47;
    return2ActivePreset();
    key5Pressed = false;
  }

  if (key6Pressed)
  {
    MIDI.sendProgramChange(47, 1);
    midiLed();

    foot6();
    bank8Presets();

    activePreset = 48;
    return2ActivePreset();
    key6Pressed = false;
  }
}

//BANK 9:

void bank9()
{
  if (key1Pressed)
  {
    MIDI.sendProgramChange(48, 1);
    midiLed();

    foot1();
    bank9Presets();

    activePreset = 49;
    return2ActivePreset();
    key1Pressed = false;
  }

  if (key2Pressed)
  {
    MIDI.sendProgramChange(49, 1);
    midiLed();

    foot2();
    bank9Presets();

    activePreset = 50;
    return2ActivePreset();
    key2Pressed = false;
  }

  if (key3Pressed)
  {
    MIDI.sendProgramChange(50, 1);
    midiLed();

    foot3();
    bank9Presets();

    activePreset = 51;
    return2ActivePreset();
    key3Pressed = false;
  }

  if (key4Pressed)
  {
    MIDI.sendProgramChange(51, 1);
    midiLed();

    foot4();
    bank9Presets();

    activePreset = 52;
    return2ActivePreset();
    key4Pressed = false;
  }

  if (key5Pressed)
  {
    MIDI.sendProgramChange(52, 1);
    midiLed();

    foot5();
    bank9Presets();

    activePreset = 53;
    return2ActivePreset();
    key5Pressed = false;
  }

  if (key6Pressed)
  {
    MIDI.sendProgramChange(53, 1);
    midiLed();

    foot6();
    bank9Presets();

    activePreset = 54;
    return2ActivePreset();
    key6Pressed = false;
  }
}

//BANK 10:

void bank10()
{
  if (key1Pressed)
  {
    MIDI.sendProgramChange(54, 1);
    midiLed();

    foot1();
    bank10Presets();

    activePreset = 55;
    return2ActivePreset();
    key1Pressed = false;
  }

  if (key2Pressed)
  {
    MIDI.sendProgramChange(55, 1);
    midiLed();

    foot2();
    bank10Presets();

    activePreset = 56;
    return2ActivePreset();
    key2Pressed = false;
  }

  if (key3Pressed)
  {
    MIDI.sendProgramChange(56, 1);
    midiLed();

    foot3();
    bank10Presets();

    activePreset = 57;
    return2ActivePreset();
    key3Pressed = false;
  }

  if (key4Pressed)
  {
    MIDI.sendProgramChange(57, 1);
    midiLed();

    foot4();
    bank10Presets();

    activePreset = 58;
    return2ActivePreset();
    key4Pressed = false;
  }

  if (key5Pressed)
  {
    MIDI.sendProgramChange(58, 1);
    midiLed();

    foot5();
    bank10Presets();

    activePreset = 59;
    return2ActivePreset();
    key5Pressed = false;
  }

  if (key6Pressed)
  {
    MIDI.sendProgramChange(59, 1);
    midiLed();

    foot6();
    bank10Presets();

    activePreset = 60;
    return2ActivePreset();
    key6Pressed = false;
  }
}
